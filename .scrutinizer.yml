before_commands:
    - 'composer install -d api'
    - 'bower install'

filter:
    excluded_paths:
        - app/
        - src/Sententiaregum/CoreDomain/Tests/

tools:
    php_code_sniffer:
        config:
            standard: PSR2
        filter:
            paths: ['api/src']
    php_loc:
        enabled: true
        excluded_dirs: [api/vendor, api/app, api/features]
    php_cpd:
        enabled: true
        excluded_dirs: [api/vendor, api/app, api/features]
    php_mess_detector: true
    php_analyzer: true
    php_pdepend: true
